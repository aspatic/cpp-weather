#include "_public.h"

void EXIT(int sig);

int main(int argc, char* argv[]){
    // print help messages

    // ignore all IO and sys/signal

    // get file datetime

    // open dir

    // traverse all files in dir
    while(1){
        
    }
    
    return 0;
}


void EXIT(int sig)  
{
  printf("程序退出，sig=%d\n\n",sig);

  exit(0);
}
